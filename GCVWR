#include <iostream>
using namespace std;

int main()
{
  int g, t, n, items, max_Weight=0, items_Weight=0;
  cin >> g >> t >> n;
  
  for (int i=0; i<n; i++)
  {
    cin >> items;
    items_Weight += items;
  }
  
  max_Weight = (g - t) * 0.9 - items_Weight;
  cout << max_Weight;
}
